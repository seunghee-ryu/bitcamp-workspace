package com.eomcs.basic.ex06;

public class Exam0400 {
  public static void main(String[] args) {

    /*
    loop:
    for (int i = 2; i <= 9; i++) {
      for (int j = 1; j <= 9; j++) {
        System.out.printf("%d * %d = %d\n", i, j, i * j);
        if (i == 5 && j == 5) {
          continue loop;
        }
      }
      System.out.println("--------------------");
    }

    */
/*
    int[] scores = new int[5];
    scores[0] = 100;
    scores[1] = 90;
    scores[2] = 80;
    scores[3] = 70;
    scores[4] = 60;

    for (int i = 0; i <scores.length; i += 2) {
      System.out.println(scores[i]);

    for (int value : scores) { //배열을 처음부터 끝까지 출력할 수 있다.
      System.out.println(value);

    }




    ArrayList<String> scores = new ArrayList<String>(); // <>안에 제약조건을 걸 수 있다.
    scores.add(100);
    scores.add(3.14f);
    scores.add("오호라");
    scores.add(true);
    scores.add('헐');
    scores.add(new Date()); // 컨트롤 쉬프트 O

    for (Object value : scores) {
      System.out.println(value);
    }
*/






  }

}

