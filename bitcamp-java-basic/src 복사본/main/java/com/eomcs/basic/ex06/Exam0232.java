package com.eomcs.basic.ex06;

public class Exam0232 {
  public static void main(String[] args) {
    // 스위치 값으로 가능한 데이터 타입은
    // 4바이트를 넘지않는 int정수, 문자열, Enum
    // 케이스 값으로 변수를 사용할 수 없다.

    // 4 바이트를 넘어가는 정수는 사용할 수 없다.
    // long,

    // 부동소수점은 사용할 수 없다
    // float, double

    // boolean 값을 스위치와 케이스에 사용할 수 없다.
    /*
     * boolean bool = true;
     * switch (bool) {
     *   case true:
     *   case false:
     *}
     */
  }

}
